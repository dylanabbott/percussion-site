{
  "name": "percussion-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:eleventy": "npx @11ty/eleventy --serve --quiet",
    "watch:sass": "sass -w src/style:src/style",
    "dev": "npm-run-all --parallel watch:*",
    "build": "npx @11ty/eleventy && sass src/style/style.scss src/style/style.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@11ty/eleventy-upgrade-help": "^2.0.5"
  }
}
